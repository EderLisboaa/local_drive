{
  "name": "local_drive",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "tsc",
    "start:dev": "npx tsnd --respawn --transpile-only src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/multipart": "^8.1.0",
    "fastify": "^4.25.2",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
